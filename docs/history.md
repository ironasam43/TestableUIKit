# 変更履歴

> 新規作成（2026-06-02）。これまでの作業は handoff_1.md より要約。

## 2026-06-25 STEP3 タスク5 — Bundle ID 正式値確定（dev.plateworks.* / commit `0c025dc`）
- HUMAN 判断により Bundle ID を `dev.plateworks.*` namespace に確定。`project.yml` 2箇所（`dev.plateworks.TestableUIKit` / `dev.plateworks.TestableUIKitDemo`）＋ `.github/workflows/ci.yml` の simctl launch ターゲットを整合更新。`docs/design.md` の placeholder ADR を Resolved 化、`docs/roadmap.md` STEP3 タスク5 を ✅ 化。これで STEP3 全7タスク完走・inbox `2026-06-25-step3-distribution-dx.md` を削除しクローズ。

## 2026-06-25 STEP3 配布・DX 整備の完走（inbox 消化）
- inbox `2026-06-25-step3-distribution-dx.md`（HQ 発・消化モード: 直接）を消化。roadmap STEP3 のタスク 1〜7 のうち 6 件を完了。**5（Bundle ID 正式値確定）は HUMAN 判断待ち**（命名方針を `[ASK HUMAN]` で確認中・現状 `com.testable.*` 維持）。
- **タスク6（堅牢化）**: `Sources/TestableUIKit/TestableServer.swift` に `State` enum（setup/ready/failed/cancelled）＋ `onStateChange` ハンドラ＋ `stop()` graceful shutdown を追加。ポート 8888 競合（EADDRINUSE）を `.failed` でプログラム的に検知可能化（従来は print のみ）。接続トラッキングで stop() 時に in-flight 接続も一括キャンセル。後方互換維持（既存 init シグネチャ不変）。XCTest +4（`testStart_emitsReadyState` / `testStop_emitsCancelledState` / `testStop_isIdempotent` / `testPortConflict_emitsFailedState`）→ **swift test 104 PASS**。
- **タスク1**: `docs/getting-started.md` 新規（5分計装ガイド・Core/AnyTestable/Registry 注入の 3 部品・既知の罠①Registry 共有もれ②testID 重複③ポート競合を明記）。
- **タスク2**: `docs/troubleshooting.md` 独立化（SETUP.md / README から抽出・拡充。サーバ起動/接続・ポート競合・多重起動・Registry 共有もれ・実機 LAN・Python ランナー）。README 旧 Troubleshooting 節はポインタ化。
- **タスク3**: GitHub Release `v0.1.0` 作成（既存 push 済みタグから）。https://github.com/ironasam43/TestableUIKit/releases/tag/v0.1.0
- **タスク4**: `README.md` 冒頭に getting-started バナー＋ドキュメント表を追加。
- **タスク7**: `Example/`（`MyToggleExample.swift` ＋ `README.md`）新規。Package ビルド対象外のコピペ用最小サンプル（トグル計装の 3 部品完結）。
- **roadmap 更新**: STEP3 を ✅ 完了へ、進捗サマリー（注記・ルート図）も整合更新。STEP2「SwiftUI 非対応」表記は project 側 roadmap には不在（既に ✅ 超過達成表記）。当該ドリフトは `Dev/docs/test-strategy.md` §7（スコープ外）に存在。
- DoD: swift test 104 PASS（+4）/ pytest unit 59 PASS（baseline 維持）。pytest の 37 errors は live DemoApp 必須の integration（環境要因・回帰なし）。

## 2026-06-25 verify-queue.template.md symlink 配布（HQ 直接改修・doc-only）
- `docs/verify-queue.template.md` を Dev/docs から相対 symlink で配布（全PJ完全パリティ）。書式正本への追従が自動化された。

## 2026-06-25 verify-queue 書式正規化（HQ 直接改修・doc-only）

- `docs/verify-queue.md` をワークスペース統一書式へ整形（HUMAN 合意・HQ＝Dev/ タブが直接実施）。正規化ヘッダ（書式ガイド＋3状態モデル・**退避2日**）を導入し、MCP Swift 版 4ツール live パリティの起票行を新書式（操作/着眼/合格 の3クローズ＋`└ 詳細`、build に紐づかない残余はラベル太字で代替）へ整形。起票内容は不変で体裁のみ。

## サマリー

- **M-1 IPC層検証** ✅: JSONValue（single value container）/ AnyTestable / TestableServer（NWListener, strict-concurrency PASS）/ CLI executable 実装。`swift build` / `swift test`(6) / `run_test.py` で wire format 実証。
- **M-2 iOS App UI連動** ✅: pbxproj 重複 isa 修復、LoginButton 実装、Simulator 上で getState→tap→getState の状態変化（isEnabled true→false）実証。ipc-protocol.md / design.md 整備。
- **M-3a xcodegen 移行** ✅: project.yml 宣言的管理へ移行、pbxproj 手編集廃止。
- **M-3-0 git cleanup** ✅: `.build/` `DerivedData/` は git 非追跡を確認。
- **M-3-1 OS/device matrix 確定** ✅: iOS 16.0 / device matrix = iPhone 16 + iPad Air（Human 確定）。
- **M-3-3 CI/CD 統合** ✅: GitHub Actions workflow 作成・green PASS。
- **M-4 setProperty 拡張** ✅: isEnabled/title に加え isHidden/alpha/backgroundColor 追加。合計 23 tests PASS。
- **M-5 テストランナー統合（進行中）**: Phase 0 PoC（probe-simulator job）修正完了（commit 5de08ca + 0f23586：iPhone UDID 選択 / bootstatus オプション修正）。CI push → **全3ジョブ ✅ PASS**（Swift Package Unit Tests / Build iOS DemoApp / Phase 0 PoC Simulator Boot App Launch & Loopback）。実 API 仕様を run_test.py 全行照合で確定（GET /ping, POST /perform で discriminate）。Phase 1 着手待機中。
- **M-5+ 実用化ロードマップ確定** ✅: `docs/roadmap.md` を新規作成。実用化の定義・現在地（到達済み）・STEP 1-4（テストランナー昇格→SwiftUI対応→DX整備→実機対応）・推奨ルート（手戻り最小）を明文化。STEP 1/2/4 は既存設計書 B/C/D に1対1対応、STEP 3 は新規カテゴリ。
- **STEP 2 実 SwiftUI コンポーネント計装** ✅（2026-06-22）: `Counter` コンポーネント（testID: `scene.demo.counter`）を `AnyTestable` 準拠で計装。`CounterCore.swift`（純粋関数: makeCounterDescribedState / applyIncrement / applyDecrement / applyCounterReset / applyCounterSetProperty）を Core 層追加、`CounterView.swift`（Counter クラス ＋ SwiftUI View）を DemoApp 層追加。`DemoApp.swift` の `register` に `await` 付与（actor 呼び出し修正）、Counter を Registry 登録。対応する pytest ファイル `Tests/test_swiftui_counter.py`（30テスト：getState/increment/decrement/reset/setProperty/状態遷移シナリオ）を追加。SPM `swift test` 33 PASS ＋ Xcode iOS Simulator ビルド BUILD SUCCEEDED で完了ゲート通過。
- **STEP 2 CI 統合** ✅（2026-06-22）: `.github/workflows/ci.yml` の pytest-ipc ジョブに `Tests/test_swiftui_counter.py` を追加（test_ipc.py と同一シミュレータセッションで併走）。GitHub Actions CI（run #27926198101）で test_ipc.py 10テスト ＋ test_swiftui_counter.py 21テスト = **合計 31 PASS**。Counter IPC（getState/increment/decrement/reset/setProperty/状態遷移）の機械検証完了。VQ 該当行を消し込み。※件数ドリフト修正：文書記載「30テスト」→実体「21テスト」。
- **STEP 3 DX整備 — `.testable(_:)` ViewModifier 抽象化** ✅（2026-06-22）: `Sources/TestableUIKit/TestableView.swift` 新規追加（`TestableRegistrationModifier: ViewModifier` ＋ `View` 拡張 `.testable(_:)`。`.task` 内で `await TestableRegistry.shared.register` を自動呼び出し）。`DemoApp/DemoApp.swift` を移行（Counter の手動 `register` 削除 → `CounterView` 配置に `.testable(counter)` 付与、testID `scene.demo.counter` 維持・後方互換）。`Tests/TestableUIKitTests/TestableRegistryTests.swift` 新規追加（register→find round-trip を async XCTest 4ケースで L1 検証）。`docs/design.md` セクション C 更新（① ViewModifier 実装済み・② EnvironmentObject は次タスクへ deferred と明記）。SPM `swift test` **37 PASS**（旧 33 + 新規 4）。commit: `1b23042`。VQ 起票なし（IPC E2E は CI で機械検証済み）。
- **STEP 4 TestableRegistry シングルトン廃止・Environment キー注入** ✅（2026-06-22）: グローバルシングルトン `TestableRegistry.shared` を廃止し、`EnvironmentKey` カスタムキー方式（`\.testableRegistry`）で Registry を SwiftUI View ツリーへ注入する設計へ移行（Design C ② 実装）。`TestableEnvironment.swift` 新規（`TestableRegistryKey: EnvironmentKey` ＋ `EnvironmentValues.testableRegistry` 拡張）。`TestableServer` を `init(port:registry:)` で Registry 注入受け取り形式に変更。`TestableRegistrationModifier` に `@Environment(\.testableRegistry)` を追加し注入 registry へ register。`DemoApp.swift` は `@State registry = TestableRegistry()` を起点に `.environment(\.testableRegistry, registry)` で View ツリー全体へ注入し、`.task` 内で同一 registry を Server へ注入。CLI `main.swift` はローカル registry を直接生成して Server へ注入。`TestableRegistryTests.swift` を `.shared` からローカルインスタンス生成へ改修し、`testMultipleInstancesAreIsolated`（複数インスタンス隔離実証）を追加。SPM `swift test` **38 PASS**（旧 37 + 新規 1）。`docs/design.md` セクション C ② を「実装済み」へ更新（EnvironmentObject 非採用理由・設計アーキテクチャ・defaultValue リスクを明記）。commit: `3718c98`。VQ 起票なし（変更は全て SPM テスト＋CI pytest で機械検証）。
- **STEP 4.5 loginButton ViewModifier 移行** ✅（2026-06-22）: `DemoApp` の `loginButton` コンポーネントを手動 `await registry.register(loginButton)` から `.testable(loginButton)` ViewModifier へ移行。RootView の `.task` 内で手動登録を廃止し、Counter と登録方式を統一（宣言的自動登録）。変更：`DemoApp/DemoApp.swift` RootView `.task` 内の `await registry.register(loginButton)` + コメント削除（L:29-30）、ContentView の loginButton VStack に `.testable(loginButton)` 付与（L:82 閉じ括弧直後）。testID `scene.demo.login.button` 維持・後方互換保証。SPM `swift test` **38 PASS** 維持（デグレなし）。`docs/design.md` セクション C に補追（loginButton 移行完了を明記）。VQ 起票なし（testID 不変で CI pytest 31 PASS 機械検証済み）。
- **STEP 2 追加実証 — 多様コンポーネント計装（TextInput / OnOffSwitch / RangeSlider）** ✅（2026-06-22）: Counter・LoginButton の 2 コンポーネント実装に続き、String 入力・Bool toggle・Double レンジの 3 コンポーネントを新規計装し、フレームワーク汎用性を実証。**Core 層**（3 ファイル新規）：`TextInputCore.swift`（text/placeholder/isEnabled 状態）、`OnOffSwitchCore.swift`（isOn/label/isEnabled 状態）、`RangeSliderCore.swift`（value/minValue/maxValue 状態）各々が純粋関数で describedState 生成・コマンド適用。**XCTest 追加**（3 ファイル新規）：`TextInputCoreTests.swift`（14 テスト：初期値/clear/setProperty/型不一致/未知キー）、`OnOffSwitchCoreTests.swift`（14 テスト）、`RangeSliderCoreTests.swift`（14 テスト）で L1 機械検証完了。**DemoApp 層**（3 ファイル新規）：`TextInputView.swift`（`AnyTestable` 準拠 ObservableObject ＋ SwiftUI View）、`OnOffSwitchView.swift`、`RangeSliderView.swift` 各々が既存 Counter パターンに統一。**DemoApp.swift 更新**：`@StateObject` ×3 追加、ContentView に各 View ＋ `.testable()` 付与（手動 register 不使用）。testID：`scene.demo.textInput`、`scene.demo.onOffSwitch`、`scene.demo.rangeSlider`。**pytest 追加**：`Tests/test_swiftui_new_components.py` 新規（TextInput/OnOffSwitch/RangeSlider 各々× getState ＋ コマンド実行で疎通確認）。**CI 更新**：`.github/workflows/ci.yml` の pytest run コマンドに新ファイル追加。SPM `swift test` **86 PASS**（旧 38 + 新規 Core テスト 48）確認。`docs/design.md` 計装済みコンポーネント一覧表追記（5 コンポーネント一覧・STEP 2 時点）。VQ 起票なし（Core ロジックは XCTest で機械検証済み、IPC 疎通は pytest で機械検証）。
- **pytest E2E 回帰 3 連修正 → main green 復帰** ✅（2026-06-22）: STEP 2 追加実証（`c71bf1a`）push 後、新規 pytest E2E（`Tests/test_swiftui_new_components.py`）が CI で red 化。初実走の IPC E2E が機械検証として機能し、**3 件のテスト専用バグ**を順次検出・修正（フレームワーク本体は全工程不変）。①`8e5695b`: fixture 3 種（text_input/on_off_switch/range_slider_perform）を counter_perform 契約へ統一（success/state エンベロープ取り違え → 生 describedState dict + raise_for_status）→ 35/37。②`0ccf6d4`: `assert_range_slider_state` の expected_keys に `step` 追加（実 describedState は4キー {value,minValue,maxValue,step}）→ 36/37。③`20896b1`: `test_range_slider_reset` 期待値を実装の中央値リセットセマンティクス `(maxValue−minValue)/2+minValue`（デフォルト 50）へ修正（テストの minValue 期待が誤り）→ **37 PASS**。**CI run #17（`27937732375`）success 確定**：pytest **37 passed**（test_ipc 10 + counter 21 + new_components 6）・全3ジョブ success（Build iOS DemoApp / Swift Package Unit Tests / IPC Integration Tests）。SPM `swift test` 86 PASS は全工程で不変（本体未変更）。VQ 起票なし（CI pytest で機械検証完結）。教訓：ローカル swift test では IPC E2E が走らない（常駐サーバ不在）＝ CI 専用レーン。新規 E2E はテスト側の応答契約・期待値ズレが出やすく、CI 初実走が検証の要。
- **STEP 1.5 MCP ラッパー化 — TestableUIKit 同梱独立 MCP サーバ実装** ✅（2026-06-22）: TestableUIKit の HTTP IPC（localhost:8888）を MCP tool で薄くラップする独立 MCP サーバを当リポ内に実装。**新規ファイル**：`mcp_server/ipc_helpers.py`（純粋ヘルパー：`build_base_url` / `build_perform_payload` / `passthrough_state` / `build_simctl_screenshot_command`・外部依存なし）、`mcp_server/testableui_mcp.py`（FastMCP サーバー・4ツール：`ui_ping` → GET /ping / `ui_getState` → POST /perform getState / `ui_perform` → POST /perform / `ui_screenshot` → simctl io booted screenshot）、`mcp_server/requirements.txt`（mcp>=1.0.0, requests>=2.28.0）。**テスト追加**：`Tests/unit/test_mcp_helpers.py`（L1 pytest 29 PASS・Simulator/DemoApp/HTTP 接続不要）、`Tests/unit/conftest.py`（autouse reset_state の no-op override で IPC 接続干渉防止）。**CI 更新**：`.github/workflows/ci.yml` に `Tests/unit/` を pytest unit ステップとして追加（Simulator 不要フェーズに分離）。**設計原則**：薄いラッパー（ビジネスロジック不持ち）、describedState は不透明 dict passthrough（STEP2 前方互換）、testID 一覧取得は将来課題。**DoD 通過**：L0 構文 OK + swift test 86 PASS 維持 + pytest unit 29 PASS + commit `5399849`。MCP 駆動の実機 PoC 駆動実証（ui_ping 死活 + DemoApp 1コンポーネント駆動）は VQ へ起票。outbox 依頼（`2026-06-22-mcp-wrapper-decision.md`）消化完了・積み残し解消。
- **Design D 下地 — LAN 越し IPC bind ホスト指定 & 接続先環境変数上書き** ✅（2026-06-22）: TestableUIKit の HTTP IPC を localhost 固定から LAN 公開可能に拡張する純コード下地を実装。**サーバー側（Swift）**: `TestableServer` の `init` に `host: String = "127.0.0.1"` 引数を追加。`NWParameters.requiredLocalEndpoint` で bind アドレスを制御（"127.0.0.1"=ループバック限定 / "0.0.0.0"=全インターフェース LAN 公開）。`start()` の print 文を `host:port` 反映に修正。`public let host: String` プロパティとして公開。後方互換維持（既存の `TestableServer(port: 8888, registry: registry)` 呼び出しは引数省略で動く）。**クライアント側（Python）**: `ipc_helpers.py` に `resolve_ipc_host_port(env:)` 純粋関数を追加（`TESTABLE_IPC_HOST` / `TESTABLE_IPC_PORT` 環境変数で接続先上書き可・既定 localhost:8888）。`testableui_mcp.py`・`Tests/conftest.py`・`run_test.py` の BASE_URL / IPC_BASE を `resolve_ipc_host_port()` 経由に変更（env 未設定で従来動作維持）。**テスト**：`Tests/TestableUIKitTests/TestableServerTests.swift` 新規（10 テスト: 既定値・カスタム host・後方互換検証）。`Tests/unit/test_mcp_helpers.py` に `TestResolveIpcHostPort` 13 テスト追加。**DoD 通過**: `swift test` 96 PASS (+10) / pytest unit 42 PASS (+13) / commit `7d81160`。**後方互換注意点**：既定 bind を 127.0.0.1（loopback）に統一。旧実装は print のみ "localhost" と表示し実際は全インターフェースバインドだったが、本変更でランタイムも loopback 限定に統一（よりセキュア・既存接続元は全 localhost で実被害なし。`docs/design.md` §D に明記）。**実機 Wi-Fi 越し実通信（実 iPhone 接続・署名・Provisioning）はスコープ外**—`docs/verify-queue.md` に「コード下地 完了・実機ペアリング後に Wi-Fi 越し実通信を確認」として起票。

- **STEP D: DemoApp DEBUG 限定 LAN 公開** ✅（2026-06-23）: `DemoApp/DemoApp.swift` の `TestableServer` 起動を `#if DEBUG` 分岐に変更し、DEBUG ビルドでは `host: "0.0.0.0"`（全インターフェース LAN 公開）、Release ビルドでは既定 `127.0.0.1`（loopback 限定）を維持。Design D §D「LAN 公開は DEBUG 限定で安全側」「Release は loopback 維持」の方針に基づく実装。**セキュリティ設計意図（重要）**: `0.0.0.0` bind は LAN 上の任意端末から test IPC（`/perform`・`/ping`）を叩けることを意味する。Release ビルドで loopback に限定することで、App Store 配布・本番運用時の意図しない外部 IPC 露出を防ぐ。DEBUG 限定公開は開発者が Wi-Fi 越しに自動テストを実行するためのみを想定。**変更スコープ**: `DemoApp/DemoApp.swift` 1ファイル・5行追加（`#if DEBUG` ブロック）のみ。ライブラリ API（`TestableServer(port:host:registry:)` 引数）は既存実装（`7d81160`）を活用・変更不要。**DoD 通過**: `swift build` BUILD SUCCEEDED / `swift test` 96 PASS（回帰なし）/ commit `b66a831`（`wip:` プレフィックス・実機確認未済）。**実機への再ビルド・再インストール・Wi-Fi 越し疎通確認はスコープ外**—Human が実機へ DEBUG ビルドを再インストール後、Mac 側で `TESTABLE_IPC_HOST=192.168.0.181` を設定して `ui_ping` を実行する手順を VQ に追記。

- **`ui_screenshot` 実機対応 — `GET /screenshot` エンドポイント ＋ screenshotProvider 注入方式** ✅（2026-06-23）: `ui_screenshot()` の実機非対応（simctl 専用）を解消。**Swift 側**：`TestableServer` に `screenshotProvider: (@MainActor () async -> Data?)?` 引数追加（既定 nil・後方互換）、`GET /screenshot` ルート追加（provider 呼出→base64→`{"image_base64","format":"png"}`、未注入時は 503 error JSON）。**DemoApp 側**：key window キャプチャ closure（`UIGraphicsImageRenderer` ＋ `UIApplication.shared.connectedScenes` で最前面 UIWindowScene の rootViewController.view をレンダリング）を生成し `TestableServer` 初期化に注入（UIKit 依存は app 側に閉じ、ライブラリは状態レス維持）。**Python 側**：`ipc_helpers.py` に `build_screenshot_url()` ・`should_use_simctl_fallback()` 追加、`testableui_mcp.py:ui_screenshot()` を `GET /screenshot` 一次経路へ変更（loopback かつ endpoint 不達時のみ simctl へフォールバック）。**L1 テスト**：Swift 4 テスト追加（stub provider で `GET /screenshot` が base64 を返すこと）、pytest 17 テスト追加（`build_screenshot_url`・フォールバック判定・passthrough）。**DoD**：`swift test` 100 PASS / pytest 59 PASS / commit `78716fd`。実機での実 PNG 取得確認（DemoApp 再インストール後）は VQ へ起票。

- **STEP 1.5 MCP live PoC — 実機 3/4 ツール駆動実証 ＋ 全コミット push・CI green** ✅（2026-06-23）: Design D で実機（iPhone `192.168.0.181`・DEBUG ビルド `b66a831`・`0.0.0.0:8888` リッスン）が Wi-Fi 越し疎通したのを受け、MCP tool 関数（`mcp_server/testableui_mcp.py`）を `TESTABLE_IPC_HOST=192.168.0.181` で直接インポート駆動。**結果（一次情報）**：`ui_ping`→`{'status':'ok'}` / `ui_getState("scene.demo.counter")`→`{'isEnabled':True,'count':0}`（describedState dict）/ `ui_perform(...,"increment")`→`count` **0→1**（実機 UI が実際に変異）/ 再 `ui_getState`→`{'count':1}`。**MCP→HTTP IPC→実機 UI 変異→describedState 反映の e2e を実機で実証**（3/4 ツール green、消し込み）。**設計ギャップ発見**：`ui_screenshot` は `simctl io booted screenshot`（Simulator 専用）ハードコードのため実機では原理的に取得不可。4/4 ではなく 3/4 にとどまるため、screenshot を VQ 未確認に別行で分離（残作業：(a) Simulator 限定で合格扱い／(b) `devicectl` 等で実機対応経路を新規実装）。**VQ 整備**：前ターンで誤って単純削除されていた Design D ✅ 行を消し込み欄へ復元（commit `1da2d34`）。**push・CI**：未 push だった全 8 コミット（`5399849`〜`1da2d34`）を `git push origin main`（`a9e1dce..1da2d34`）→ GitHub Actions CI run `27989218172` **全3ジョブ success**（Build iOS DemoApp 36s / Swift Package Unit Tests 34s / IPC Integration Tests pytest 2m54s）。

- **`ui_screenshot` 実機 PNG 取得確認 — 経路A 実機 e2e 成立（VQ クローズ）** ✅（2026-06-23）: 最新 DEBUG ビルド（`05f03b7` ベース・`GET /screenshot` 搭載）を実機（iPhone `192.168.0.181`）へ再インストール・起動済み。Mac から `TESTABLE_IPC_HOST=192.168.0.181 .venv/bin/python` で実コード `mcp_server/testableui_mcp.ui_screenshot()` を直接駆動（再実装せず実関数を実行）。**結果（一次情報）**：戻り値 `format:"png"`・`image_base64`（base64 106,360 文字）取得 → base64 デコード後 **79,768 bytes**・先頭 8 バイトが **PNG シグネチャ `\x89PNG\r\n\x1a\n` に一致**・IHDR 寸法 **960×1440**。`GET /screenshot`（screenshotProvider 注入＝アプリ内 `UIGraphicsImageRenderer` キャプチャ）経路が **Simulator 非依存で実機動作することを実証**。これにより実機テスト基盤（ping/getState/perform/screenshot の 4/4 ツール）が実機で完全クローズ。`docs/verify-queue.md` の最後の未確認 1 件を消し込み（未確認 0 件）。コード変更なし（検証のみ）。

- **運用ドキュメント整備 — SETUP.md ステップ7・README.md 実機実行サブ節追加** ✅（2026-06-23）: 実機テスト基盤（4/4 ツール ping/getState/perform/screenshot）が完全クローズしたが、対外ドキュメントが Simulator 前提のまま取り残されていた。**SETUP.md 更新**：新節「ステップ7：実機（LAN 越し）での実行」を追加（前提条件・LAN 公開の仕組み・Xcode コンソール確認・実機 IP 調べ方・DHCP 変動注意・`TESTABLE_IPC_HOST` 環境変数設定・screenshot 二経路説明・実機向けトラブルシューティング）。**README.md 更新**：(1) 「使用方法 > テスト実行」に「実機（LAN 越し）での実行」サブ節を追加（SETUP.md ステップ7への参照）。(2) 「次のフェーズ」バックログの実機対応行を `[x] 実機対応（Wi-Fi 越し LAN 通信）✅` へ更新。(3) アーキテクチャ図の `iOS Simulator（DemoApp）` → `iOS Simulator / 実機（DemoApp）` に1行修正。**命名・分岐条件確認**：env 名 `TESTABLE_IPC_HOST`/`TESTABLE_IPC_PORT`（`ipc_helpers.py` 定義）、ログ文字列 `✅ TestableServer listening on http://0.0.0.0:8888`（`TestableServer.swift:54` ）、フォールバック条件「loopback かつ接続失敗時のみ simctl」（`testableui_mcp.py:97-114`）—全て一次情報で実装コードより確認済み・矛盾なし。ドキュメント変更のみ・コード変更なし・VQ 不要。

- **roadmap.md 陳腐化是正 — STEP2/4 完了・STEP3 部分残りを実態反映**（inbox `2026-06-23-roadmap-drift-update.md` 消化）✅（2026-06-23）: `docs/roadmap.md`（最終更新 2026-06-02）が実コードより古く、HQ `Dev/docs/test-strategy.md` §7 と齟齬があった件の是正。一次情報裏取り（`.testable(_:)` modifier 実装＝`Sources/TestableUIKit/TestableView.swift:34`、4 Core ファイル `Counter/TextInput/OnOffSwitch/RangeSliderCore` 実在、`swift test` 100 / pytest 59 PASS）の上で更新。**STEP 2 → ✅ 完了（超過達成）**：`.testable` modifier・Environment 注入・4 Core 計装を実績反映。**STEP 4 → ✅ 完了（先行完走）**：実機 4/4 ツール e2e・screenshot PNG 取得確認・推奨ルート逆転の注記を追加。**STEP 3 → 🟡 部分残り**：semver タグ・`Example/`・getting-started/troubleshooting・graceful shutdown・Bundle ID 正式化を残タスク化、全PJ展開の律速と明示（§7 課題 B と整合）。**現在地（第2節）**：テスト規模・MCP 4 ツール・Registry シングルトン廃止・多様計装を反映＋進捗注記追加。**推奨ルート（第4節）**：当初計画と実際の進行（1→2→4→3）を併記。最終更新日を 2026-06-23 に更新。ドキュメント変更のみ・コード変更なし・VQ 不要。

## 2026-06-24 inbox 消化: B（MCP サーバ）Swift 化・SPM 公開の可否照会 回答（検収必要）
- HQ からの照会チケット `2026-06-24-mcp-server-swift-feasibility.md`（消化モード=フロー・検収必要）を消化。実装着手なし・可否/工数の当事者見解を回答。
- 一次情報裏取り: `testableui_mcp.py`(178行)＋`ipc_helpers.py`(純関数)＝状態レス HTTP↔MCP 中継・固有ロジックなし／`TestableServer.swift` が `/ping`・`/perform`・`/screenshot` 保持／screenshot は `GET /screenshot`(app側 provider)一次・simctl は loopback fallback／**git tag 0 件**（version pin 不可）／Package.swift 外部依存ゼロ。
- 回答要旨: ①Swift SDK で4ツール再実装は可（FastMCP 相当の自動推論は無く Tool 手動登録だが schema 単純で軽い）②Package.swift target 追加=S〜M・**外部依存ゼロ方針は MCP executable target に依存を封じ込めれば library 側は維持可** ③screenshot は既存 `GET /screenshot` provider 経路で吸収・simctl fallback は `Process` 移植可で設計不変 ④Python 版はパリティ＋テスト確立まで併走、その後廃止（恒久併存は避ける）⑤semver タグ即起票必須(S)・public API 監査は 0.x なら S/1.0 なら M。前提整理として「Swift 化＝in-process 化ではなく venv 消滅/SPM 配布/単一言語」を §6 #3 へ反映推奨。
- 回答全文は `inbox/done/2026-06-24-mcp-server-swift-feasibility.md` に追記。コード変更なし（照会回答のみ）。

## 2026-06-24 inbox 消化: semver タグ運用の導入と初期 v0.1.0 タグ起票（検収不要）
- HQ からのチケット `2026-06-24-semver-tag-adoption.md`（消化モード=直接・検収不要）を消化。
- 一次情報確認: `git tag` 0 件・`git ls-remote --tags origin` 0 件で **semver タグ実測ゼロ** を裏取り。公開 API は screenshotProvider 追加・Environment キー注入など破壊的変更が継続中で pre-1.0（`0.x`）が妥当と判定。
- 初期 annotated tag `v0.1.0` を `main` HEAD に付与し remote へ push。利用側（DeeperAI 等）が `.package(url:, from: "0.1.0")` でバージョン固定可能に。
- semver 運用方針を `README.md` に「## バージョニング方針（semver）」節として明文化（pre-1.0 では MINOR=破壊的変更／PATCH=互換変更、タグを切るタイミング、1.0 到達条件）。
- コード変更なし（タグ＋ドキュメントのみ）。L0 ビルド対象外。

## 2026-06-24 inbox 消化（フロー）: MCP サーバ B の Swift 化・SPM 公開
- HQ チケット（消化モード=フロー）を cmflow で消化。Python 製 MCP サーバ `mcp_server/` を Swift（MCP swift-sdk）へ書き直し SwiftPM 配布可能にした。公開 4 ツール・HTTP 契約・状態レス設計は Python 版と完全パリティ。
- **依存封じ込めの設計判断**: swift-sdk は実測で tools-version 6.1・iOS16・swift-system/log/nio 等 transitive 依存を要求。メイン `Package.swift`（tools 5.9・iOS15・依存ゼロ）に同梱すると library `TestableUIKit` の consumer まで巻き込み「外部依存ゼロ・iOS15 維持」を破壊する。MCP サーバは TestableUIKit の Swift 型を import せず HTTP IPC のみで通信するため、**完全に独立した sub-package `mcp-swift/`** へ分離し、メイン `Package.swift` は一切変更しない構成にした（承認 [ASK HUMAN] 選択肢1）。
- **新規 sub-package `mcp-swift/`**（tools 6.0 / macOS13）:
  - `Sources/TestableUIKitMCPCore/IPCHelpers.swift` — 純関数（外部依存ゼロ・XCTest 対象。Python `ipc_helpers.py` と 1:1）: host/port 解決・base/screenshot URL 構築・perform payload 組み立て・simctl コマンド生成・loopback 判定。
  - `Sources/TestableUIKitMCP/main.swift` — MCP `Server` に 4 ツール手動登録（ui_ping/ui_getState/ui_perform/ui_screenshot）・`StdioTransport` 起動・`URLSession` で HTTP 中継・`ui_screenshot` は `GET /screenshot` 一次／loopback のみ simctl フォールバック。swift-sdk 依存はこの executable のみ。
  - `Tests/TestableUIKitMCPCoreTests/IPCHelpersTests.swift` — L1 XCTest 41 ケース。
- **検証**: `cd mcp-swift && swift build`（警告ゼロ）・`swift test` **41 PASS**。MCP stdio handshake を Python ハーネスで実証（`initialize`→serverInfo `TestableUIKit 0.1.0`、`tools/list`→4 ツール登録確認、`tools/call ui_ping`（アプリ未起動）→`isError:true` で graceful fail＝HTTP 中継経路の配線確認）。
- **依存分離の確認**: ルート `swift build` ではメイン package グラフのみ解決され swift-sdk は出現しない（`mcp-swift/` は別パッケージ）。両グラフが交わらないことが分離成立条件。`Package.resolved` も追跡（再現性）。
- `docs/design.md` に §E2「MCP サーバ Swift 化」を追記（分離理由・ターゲット構成・起動方法・Python 版廃止条件）。
- **残**: 実アプリ応答の成功パス（起動中 DemoApp に対する 4 ツール live 駆動パリティ）は実機/Simulator 前提のため VQ へ起票。Python 版 `mcp_server/` は廃止条件（live パリティ確認＋CI 組込＋ドキュメント一本化）を満たすまで併走。
